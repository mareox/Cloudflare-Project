# Cloudflare Project

Public-ready workspace for the Cloudflare application project.

## Structure

- `docs/` - project notes, runbooks, reports, and handoff material.
- `origin-server/` - origin service code and deployment assets.
- `worker/` - Cloudflare Worker code, configuration, and edge logic.

